export interface PresaleDetails {
  presale_end_time: string;
  html_is_customized: string;
  release_id: string;
  starts_at: string;
  signup_after_presale_start: boolean;
  presale_start_date_time: string;
  presale_start_time: string;
  presale_link: string;
  presale_start_date: string;
  presale_end_date_time: string;
  short_link: string;
  custom_term_html: string;
  event_ids: number[];
  presale_end_date: string;
  presale_alerts: boolean;
  custom_term_raw: string;
  presale_code: string;
  presale_short_link: string;
}
