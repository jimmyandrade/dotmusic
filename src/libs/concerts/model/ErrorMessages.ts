export enum ErrorMessages {
  INVALID_ENV_VARIABLE = 'Environment variable is missing or invalid',
  FETCH_EVENTS_FAILED = 'Failed to fetch events from Sympla',
  INVALID_RESPONSE_DATA = 'Invalid response data format',
}
