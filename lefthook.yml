pre-commit:
  parallel: true
  commands:
    typescript-check:
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit
pre-push:
  parallel: true
  commands:
    build:
      run: npm run build
      skip:
        - merge
        - rebase
